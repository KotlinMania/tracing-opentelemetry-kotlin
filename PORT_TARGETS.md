# Port targets — `tracing-opentelemetry-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `tracing-opentelemetry` that codex-rs uses, and therefore the minimum subset that `tracing-opentelemetry-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-168] (rank `39`, tier `direct`, status `To Do`)

**Cargo package name**: `tracing-opentelemetry`
**Rust module name (in `use` statements)**: `tracing_opentelemetry`

## Summary

- **1** distinct symbols imported by codex-rs
- **4** total `use` statements
- **4** codex source files affected
- **4** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `tracing_opentelemetry::OpenTelemetrySpanExt`

- **4 `use` site(s)** across **4 codex member crate(s)**: `codex-client`, `codex-core`, `codex-exec`, `codex-otel`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/src/default_client.rs:14` (`codex-client`) — `use tracing_opentelemetry::OpenTelemetrySpanExt`
- `core/src/session/tests.rs:155` (`codex-core`) — `use tracing_opentelemetry::OpenTelemetrySpanExt`
- `exec/src/lib_tests.rs:12` (`codex-exec`) — `use tracing_opentelemetry::OpenTelemetrySpanExt`
- `otel/src/trace_context.rs:13` (`codex-otel`) — `use tracing_opentelemetry::OpenTelemetrySpanExt`
